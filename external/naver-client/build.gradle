jar.enabled = true

dependencies {
    // common 모듈에 있는 ApiException 을 끌어다 쓰기 위해
    implementation project(':common')
    // api로 의존성을 선언하면, 그 의존성은 선언된 모듈에서 사용할 수 있을 뿐만 아니라, 그 모듈에 의존하는 다른 모듈에서도 접근할 수 있습니다.
    api 'org.springframework.cloud:spring-cloud-starter-openfeign'
}